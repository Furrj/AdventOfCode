def calculate_routes(
    cities: list[str], distances: list[tuple[str, str, int]]
) -> list[tuple[str, int]]:
    routes: list[tuple[str, int]] = []
    start: str = ""
    route: list[str] = []
    distance: int = 0

    for city in cities:
        start = city

    return routes
